{
  "name": "personal-ledger-app",
  "version": "0.1.0",
  "private": true,
  "description": "个人离线记账 App",
  "scripts": {
    "serve": "node serve.js",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
