{
  "name": "personal-ledger-app",
  "version": "1.5.0",
  "private": true,
  "description": "支持本地记账、统计、备份和 AI 财务分析的安卓 App",
  "scripts": {
    "serve": "node serve.js",
    "ai:test": "node tests/ai-boundary-eval.mjs",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
